{
  "id": "openclaw-memory",
  "kind": "memory",
  "configSchema": {
    "type": "object",
    "properties": {
      "database": {
        "type": "object",
        "properties": {
          "host": {"type": "string"},
          "port": {"type": "number"},
          "database": {"type": "string"},
          "user": {"type": "string"},
          "password": {"type": "string"}
        }
      },
      "endpoint": {
        "type": "string",
        "description": "HTTP endpoint for memory service"
      }
    }
  }
}
